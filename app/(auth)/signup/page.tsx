import { Register } from '@/features/auth/components/register_form'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <Register />
    </div>
  )
}

export default page